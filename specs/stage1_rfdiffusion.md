# Stage 1 — RFdiffusion backbone generation

## Goal

Generate 200 de novo binder backbones against a cleaned pMHC-I template
using RFdiffusion's complex binder mode with peptide-centric arcing
geometry. Hotspots, peptide identity, chain map, and binder length range
are inherited from `results/cycle_NN/stage0/target.yaml` (the cycle-scoped
`TargetManifest` emitted by Stage 0's `prep_target.py`). Stage 1 produces
backbones only; sequences are designed in Stage 2.

## Anchored references

- `HADRUP_JENKINS_2025` — Fig 1A pipeline (RFdiffusion → ProteinMPNN → AF2).
  Hotspot pattern: peptide non-anchor residues. Noise scaling Cα = 0.
  ~45 s/design on A100 40 GB. MDAR Reproducibility Checklist confirms
  vanilla RFdiffusion (cites Watson et al. 2023 Nature for code).
- `BAKER_LAB_2025` — Methods + Fig 1A/1B. Peptide-centric arcing geometry.
  Data S1 confirms wt1-5 = 85 aa, wt1-8 = 86 aa, both scaffold=No
  (de novo, not partial diffusion). Baker de novo length cluster: 77–102.
  Partial diffusion with privileged mage-513 (102 aa) scaffold — DEFERRED
  to cycle 3+; not used for WT1 in the source paper.
- `HOUSEHOLDER_GARCIA_2025` — 4-helix-bundle alternative (99–129 aa).
  Not the cycle-2 strategy; informational only.
- `MARES_IOANNIDIS_2025` — confirms RFdiffusion explores high-novelty,
  binding-plausible sequence space (Fig. S12); supports diffusion-first.

## Inputs

1. **`results/cycle_NN/stage0/target.yaml`** — Stage-0 cycle-scoped
   `TargetManifest`, source of truth for chains/hotspots/length. Nested
   schema (see `workflow/scripts/prep_target.py`):

   ```yaml
   cycle: "02"
   mock: false
   primary:
     target_id: wt1_a0201
     pdb_id: 3HPJ
     cleaned_pdb: data/targets/3hpj_clean.pdb
     peptide_sequence: RMFPNAPYL
     chains:
       A: { role: heavy_chain, length: 275 }   # actual length read at runtime
       B: { role: beta2m,      length:  99 }
       C: { role: peptide,     length:   9 }
       D: { role: binder,      length: null }
     hotspots:
       peptide:
         - { token: C1, chain: C, resnum_clean: 1, resnum_rcsb: 1 }
         - { token: C4, chain: C, resnum_clean: 4, resnum_rcsb: 4 }
         - { token: C6, chain: C, resnum_clean: 6, resnum_rcsb: 6 }
         - { token: C8, chain: C, resnum_clean: 8, resnum_rcsb: 8 }
       mhc:
         - { token: A65,  chain: A, resnum_clean:  65, resnum_rcsb:  65 }
         - { token: A66,  chain: A, resnum_clean:  66, resnum_rcsb:  66 }
         - { token: A150, chain: A, resnum_clean: 150, resnum_rcsb: 150 }
         - { token: A155, chain: A, resnum_clean: 155, resnum_rcsb: 155 }
     binder:
       length_min:  70
       length_max: 110
   positive_control: { ... }   # 2BNR/Jenkins, used elsewhere
   ```

2. **`data/targets/3hpj_clean.pdb`** (gitignored; regenerated by `prep_target.py`
   on each pod). Three chains in fixed order: A=HC, B=β2m, C=peptide
   (9-mer RMFPNAPYL). Cleaned: no waters, no alt-locs, single-occupancy,
   MSE→MET, peptide chain renumbered 1..N.

3. **`configs/rfdiffusion_default.yaml`** (existing, edited in cycle 2). Pinned values:

   ```yaml
   inference:
     num_designs: 200
     ckpt_override_path: /workspace/models/RFdiffusion/Complex_base_ckpt.pt
   denoiser:
     noise_scale_ca: 0       # HADRUP_JENKINS_2025
     noise_scale_frame: 0
   contigmap:
     # Built at runtime by run_rfdiffusion._build_contigmap from cleaned-PDB
     # chain lengths + target.binder.{length_min,length_max}.
   ppi:
     hotspot_res_from_target: true
   partial_T: null          # BAKER_LAB_2025 partial diffusion deferred to cycle 3
   ```

4. **`configs/seeds.yaml`** (new) — seed formula constant:

   ```yaml
   formula: "cycle * 1000 + design_index"
   reserved:
     cycle_01: [1000, 1999]   # unused; cycle 1 had no RFdiffusion runs
     cycle_02: [2000, 2199]
   ```

5. **`/workspace/RFdiffusion/`** — repo clone (`bootstrap.sh` step [5/7]).
6. **`/workspace/models/RFdiffusion/Complex_base_ckpt.pt`** — checkpoint
   (`bootstrap.sh --with-rfdiffusion-weights`, sha256-verified).

## Outputs

```
results/cycle_NN/stage1/rfdiffusion/
├── designs/
│   ├── design_00000.pdb     # binder only; RFdiffusion writes chain "A"
│   ├── design_00001.pdb     # Stage 2 splicing renames to chain D
│   └── ...
├── designs.jsonl            # one JSON record per design
├── run.log                  # raw RFdiffusion stdout/stderr (per design or aggregated)
└── stage1_summary.json      # halt-gate inputs + verdict
```

`designs.jsonl` schema (one JSON per line):

```json
{
  "design_id": "design_00000",
  "pdb_path": "results/cycle_02/stage1/rfdiffusion/designs/design_00000.pdb",
  "target_id": "wt1_a0201",
  "binder_length": 87,
  "hotspot_residues": ["C1","C4","C6","C8","A65","A66","A150","A155"],
  "contigmap": "[A1-275/0 B1-99/0 C1-9/0 70-110]",
  "noise_scale_ca": 0.0,
  "noise_scale_frame": 0.0,
  "partial_T": null,
  "checkpoint": "Complex_base_ckpt.pt",
  "checkpoint_sha256": "...",
  "rfdiffusion_git_sha": "...",
  "seed": 2047,
  "wall_seconds": 47.2,
  "ca_contact_to_hotspots_n": 5,
  "geometry_pass": true
}
```

`stage1_summary.json` schema:

```json
{
  "cycle": 2,
  "target_id": "wt1_a0201",
  "n_attempted": 200,
  "n_completed": 198,
  "n_geometry_pass": 187,
  "fraction_geometry_pass": 0.944,
  "length_dist": {"min": 70, "max": 110, "median": 89, "p25": 78, "p75": 102},
  "seed_range": [2000, 2199],
  "halt_rule": {
    "name": "geometry_pass_rate",
    "threshold": 0.50,
    "observed": 0.944,
    "verdict": "PASS",
    "note": "Threshold is calibration-only for cycle 2; tighten in cycle 3"
  },
  "rfdiffusion_git_sha": "...",
  "weights_sha256": {"Complex_base_ckpt.pt": "..."},
  "wall_minutes_total": 150.0,
  "started_at": "...",
  "finished_at": "..."
}
```

## Quality gates

**Halt rule**: `fraction_geometry_pass >= 0.50`.

**Per-design geometry pass** (AND of three checks):

- Binder Cα to ≥3 hotspot Cα atoms within 10.0 Å (any hotspot — peptide or
  MHC, both count).
- Binder residue count within `[length_min - 5, length_max + 5]`
  (currently `[65, 115]`).
- No internal binder Cα–Cα clash < 3.5 Å (excluding the trivial sequential
  i, i+1 backbone bond).

**Threshold rationale**: 0.50 is calibration-only. We have zero empirical
data on what fraction of RFdiffusion outputs pass basic geometry for our
specific contigmap. Setting a real threshold without seeing the
distribution first risks either over-halting (compute waste on false
fails) or under-halting (garbage into Stage 2). 0.50 means "not
catastrophically broken" — if more than half fail basic geometry, the
contigmap or weights are wrong and we want to halt immediately. Tighten
in cycle 3 once the cycle-2 empirical pass rate is known. Codified in
`docs/known_traps.md`.

## Mock mode

`scripts/run_stage1.py --mock --cycle 99` runs in <5 s on CPU, no GPU.
Fixtures at `tests/fixtures/stage1/mock_design_{00000..00009}.pdb` contain
10 hand-built backbones (8 pass / 2 fail by construction at threshold
0.50: observed = 0.80, well above floor). `tests/test_stage1_halt_gate.py`
asserts pass-path AND a synthesized fail-path (force 6 fails → observed
0.40 → verdict FAIL).

## Implementation tasks

- [ ] `workflow/scripts/run_rfdiffusion.py` — native RFdiffusion subprocess
  driver. Helpers `_load_target`, `_build_contigmap` (reads actual chain
  lengths at runtime from the cleaned PDB, never hardcoded),
  `_compute_seed`, `_geometry_pass`, `_emit_designs_jsonl`,
  `_compose_summary`. Subprocess pattern (single vs per-design) is
  determined by a one-time pod-side recon of RFdiffusion's
  `inference.num_designs=N` seed-threading behaviour (documented in the
  module docstring + `docs/known_traps.md`).
- [ ] `scripts/run_stage1.py` — orchestrator mirroring
  `scripts/run_controls.py`. Flags `--cycle`, `--mock`, `--skip-subprocess`.
  `--skip-subprocess` skips both the RFdiffusion subprocess and the
  mock-fixture copy, then re-enumerates whatever `design_*.pdb` files
  already exist in `out-dir/designs/` and rewrites `stage1_summary.json`.
  This is the recovery path for the cycle-02 writer/reader filename drift
  (see `docs/known_traps.md`); it replaces the older `--metrics-only`
  flag, which only re-read an existing (potentially broken) summary and
  had no production use case. `os.environ.pop("LD_LIBRARY_PATH", None)`
  at top.
- [ ] `tests/test_run_rfdiffusion.py` — unit tests for the helpers.
- [ ] `tests/test_stage1_halt_gate.py` — orchestrator + halt-gate test
  (pass + fail directions).
- [ ] `tests/test_run_rfdiffusion_real_writer.py` — synthetic regression
  test exercising the real RFdiffusion filename convention
  (`design_{seed}.pdb`, seed = cycle\*1000 + design_index) independently
  of `_copy_mock_fixtures`. Would have caught the cycle 02 drift.
- [ ] `workflow/rules/01_rfdiffusion.smk` — overhauled to consume `target.yaml`
  + `rfdiffusion_default.yaml` + `seeds.yaml` and produce the outputs above.
  The rule's outputs are `stage1_summary.json` and `designs.jsonl`; it no
  longer asserts a per-design PDB filename as a sentinel (real-mode writer
  emits `design_{seed}.pdb`, not `design_00000.pdb`, so the previous
  `sample_pdb=...design_00000.pdb` declaration would never resolve).
- [ ] `workflow/rules/02_proteinmpnn.smk` — declare `stage1_summary.json`
  as an upstream input so the DAG chains.

### Writer/reader filename contract

The enumerator that builds `stage1_summary.json` and `designs.jsonl`
resolves on-disk PDB paths through a single helper,
`_expected_pdb_path(designs_dir, design_index, cycle, seeds, mock)`:

- In **real mode**: returns `designs_dir / f"design_{seed}.pdb"` where
  `seed = _compute_seed(cycle, design_index, seeds)`. This matches
  RFdiffusion's `inference.output_prefix={designs_dir}/design` +
  `inference.design_startnum={seed}` (un-padded, variable-digit names).
- In **mock mode**: returns `designs_dir / f"design_{design_index:05d}.pdb"`,
  matching `_copy_mock_fixtures`'s output after stripping `mock_` from
  the fixture filenames.

`_compute_seed` is the single source of truth for the seed formula
(`cycle * 1000 + design_index`, enforced against the reserved range in
`seeds.yaml`). Any change to the writer-side naming must be mirrored in
`_expected_pdb_path`, never in the call sites. See `docs/known_traps.md`
for the cycle 02 incident this contract prevents.

## Known traps inherited

1. No Docker. RFdiffusion runs as a native subprocess.
2. `uv run python` everywhere.
3. `os.environ.pop("LD_LIBRARY_PATH", None)` at the top of all new scripts
   that import JAX/PyTorch with CUDA.
4. Mock smoke tests use `--cycle 99`.
5. Mock fixtures exercise the halt rule in both directions.
6. Idempotency: skip-if-present per-design PDB; re-emit `designs.jsonl`
   from whatever's on disk.
7. Weights gated behind `--with-rfdiffusion-weights` in `bootstrap.sh`,
   sha256-verified post-download.
8. `data/targets/*_clean.pdb` gitignored (cleaned PDBs are deterministic
   outputs of `prep_target.py`; pod regenerates them).
9. **NEW (trap #13)**: Stage 1 geometry-pass threshold is calibration-only
   until cycle 3. Log empirical pass rate in `stage1_summary.json` and
   recalibrate.

## Chain ordering handoff to Stage 2

RFdiffusion writes the binder as chain "A" internally inside its output
PDB. Stage 2's downstream splicing script (later session) is responsible
for splicing the binder onto the cleaned pMHC and assigning the final
canonical layout A=HC, B=β2m, C=peptide, D=binder. Stage 1 outputs stay
in their native chain layout.

## Done criteria

- [ ] `uv run python scripts/run_stage1.py --mock --cycle 99` runs in <5 s
  and exits 0 with `verdict: PASS`.
- [ ] `pytest tests/test_stage1_halt_gate.py tests/test_run_rfdiffusion.py`
  green (pass + fail paths exercised).
- [ ] CI green on `uv sync --extra dev`.
- [ ] Real pod run, cycle 02: 200 attempts, ≥50 % pass, `stage1_summary.json`
  committed, `designs.jsonl` committed, 3–5 sample PDBs copied into
  `results/cycle_02/stage1/rfdiffusion/samples/` and committed. Bulk
  designs stay gitignored. Empirical pass rate logged for cycle 3
  calibration.

---

## Cycle 03: partial-diffusion sub-runs

Cycle 02 produced one hero out of 200 designs and a placement deficit (~65 %
of designs made zero hotspot contact). Cycle 03 replaces de-novo generation
with **partial diffusion** (BAKER_LAB_2025's privileged-scaffold-reuse
pathway) from two seeded sub-runs that merge into the canonical Stage 1
output Stage 2 already consumes. Selected by `stage1_mode` (Snakefile;
`partial` when cycle ≥ 3, overridable via `--config stage1_mode=`). A
conditional `ruleorder` makes `stage1_merge` win over the legacy
`rule rfdiffusion`; the two produce the same canonical outputs.

### Sub-run A — BAKER scaffold library (`configs/rfdiffusion_subrun_a.yaml`)

1. **Align** each BAKER `scaf*.pdb` target chain onto `3hpj_clean.pdb`
   (`workflow/scripts/align_scaffolds.py`, a BioPython `Superimposer`
   equivalent of BAKER's `align_chainB.py` — no Rosetta, no upstream-clone
   dependency).
2. **Partial-diffuse** one binder backbone per aligned scaffold
   (`workflow/scripts/partial_diffuse.py`), `partial_T = 15`,
   `noise_scale_ca = 0`. ~150 backbones.

`contigmap.contigs` is **derived per-design** from each aligned scaffold's
geometry — not statically configured — because BAKER scaffolds vary in binder
size (`partial_diffuse._derive_contigs_subrun_a`). The binder slot is a **bare
`N-N`** range (chain-A CA count; unprefixed = redesigned) and the static motif
portion is the letter-prefixed `B1-180/0 C1-9` (preserved). RFdiffusion requires
this in partial-diffusion mode as the residue mask (Trap #32). The resulting
designs are 3-chain **A=binder, B=HLA, C=peptide**; Stage 1 scores them with
`fixed_chains={B,C}` (`run_stage1_subrun._fixed_chains_for_subrun`) so the binder
on chain A is identified by exclusion.

`partial_T = 15` (vs 10 for sub-run B): BAKER scaffolds are foreign backbones
that need more remodeling to adapt to the WT1/A\*02:01 interface. This is a
conservative blind default — there is no published optimum for pMHC scaffold
reuse; recalibrate in cycle 04 from empirical pass rates.

### Sub-run B — design_2079 hero (`configs/rfdiffusion_subrun_b.yaml`)

Partial-diffuse `num_designs` (~30) binder backbones from the cycle-02 hero
**full complex** seed (`data/seeds/design_2079_binder.pdb` = the hero chain D
stitched onto `3hpj_clean.pdb` A/B/C — partial diffusion needs the target as
the fixed motif). `partial_T = 10` (gentle local remodeling that preserves
the validated placement).

### Merge (`scripts/merge_stage1_subruns.py`, `rule stage1_merge`)

Concatenates both sub-runs' `designs.jsonl`, copies PDBs into
`stage1/rfdiffusion/designs/`, and runs the geometry-pass halt gate once on
the merged set (PASS when `fraction_geometry_pass ≥ 0.10`, calibration-only;
placement quality is now enforced separately by the Stage 2 contact filter).

### Seeds (`configs/seeds.yaml`)

`cycle_03_rfdiffusion: [3000, 3199]`. Sub-run A takes indices 0–149
(3000–3149); sub-run B takes 150+ (3150+) via per-config `seed_offset`, so the
two occupy disjoint slots within the cycle's reserved range.

### Pod-only inputs

The BAKER library and the hero prediction are gitignored / pod-only.
`workflow/scripts/setup_cycle03_inputs.py` (run once on the pod after pull)
symlinks the scaffolds into `data/scaffolds/baker_library/` and stitches the
seed complex into `data/seeds/`. CC develops against mock fixtures
(`tests/fixtures/baker_library_mock/`, `tests/fixtures/design_2079_mock_seed.pdb`).

### Mock

`snakemake --config mock=true cycle=99 -j1` runs the whole partial-diffusion
DAG with no GPU: sub-runs synthesize geometry-passing 4-chain designs from the
reference motif, merge halts PASS, Stage 2 proceeds. Unit coverage in
`tests/test_align_scaffolds.py`, `tests/test_partial_diffuse.py`,
`tests/test_stage1_subruns.py`.
